import SwiftUI

struct ExtractionCreateView: View {
    var body: some View {
        Text("抽出メソッド作成画面")
            .font(.title)
            .navigationTitle("作成")
            .navigationBarTitleDisplayMode(.inline)
    }
}
